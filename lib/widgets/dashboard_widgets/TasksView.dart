import 'package:flutter/material.dart';

// Will probably make this a seperate screen and serve as general task management (add, remove, move)
// Widget that displays the different tasks and allows users to prioratize and mark progress
// Will need to recieve list of task objects

class TaskView extends StatefulWidget {

  @override
  _TaskViewState createState() => _TaskViewState();
}

class _TaskViewState extends State<TaskView> {
  @override
  Widget build(BuildContext context) {
    return Text("Building");
  }
}
